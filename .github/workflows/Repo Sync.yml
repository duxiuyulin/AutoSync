# File: .github/workflows/repo-sync.yml
name: Repo Sync
on: 
  schedule: 
  - cron: '0 0 * * *' #此处为同步运行时间
  workflow_dispatch: 
  watch: 
   types: started 
  repository_dispatch:
    types: Repo Sync
jobs: 
 repo-sync: 
     env: 
       PAT: ${{ github.event.client_payload.PAT || secrets.PAT }} #此处PAT需要申请，教程详见：https://www.jianshu.com/p/bb82b3ad1d11
     runs-on: ubuntu-latest 
     if: github.event.repository.owner.id == github.event.sender.id 
     steps: 
      - uses: actions/checkout@v2 
        with: 
          persist-credentials: false 
    
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/qitoqito/kedaya.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "kedaya" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }} 
          
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/JDHelloWorld/jd_scripts.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "JDHelloWorld" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }}  
               
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/star261/jd.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "star261" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }} 
                
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/smiek2221/scripts.git" # 上游仓库地址
          source_branch: "master" 
          destination_branch: "smiek2221" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }}       

      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/jiulan/platypus.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "jiulan" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }} 
                
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/Tsukasa007/my_script.git" # 上游仓库地址
          source_branch: "master" 
          destination_branch: "Tsukasa007" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }}   
              
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/zero205/JD_tencent_scf.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "zero205" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }}     
            
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/KingRan/JD-Scripts.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "KingRan" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }}       
          
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/ccwav/QLScript2.git" # 上游仓库地址
          source_branch: "main" 
          destination_branch: "ccwav" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }} 
          
      - name: Repo Sync
        uses: repo-sync/github-sync@v2 
        if: env.PAT 
        with: 
          source_repo: "https://github.com/Aaron-lv/sync.git" # 上游仓库地址
          source_branch: "jd_scripts" 
          destination_branch: "Aaron-lv" # 按照自己仓库分支名称修改
          github_token: ${{ github.event.client_payload.PAT || secrets.PAT }}
